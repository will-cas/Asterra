# Asterra icons (Resources)

White tint-friendly PNGs from Kenney Board Game Icons / Game Icons (CC0).

`HudStyle.GetIcon` / `Portrait` load `Resources.Load("Asterra/Icons/<key>")` and multiply by accent when caching. Required default: `portrait.png`.
